说明：


1 一个类似于 nttrace的程序，支持 x64
2 不同的是可以指定过滤调用 DLL和被调用DLL，这样的话可以指定
某个模块调用 ntdll.dll的函数。过滤了许多的无效调用。
3 可以过滤dll名和函数。
4 可以查看跟踪函数的调用栈。
5 可以查看函数的参数。
6 出厂带了一个配置过滤了一些 DLL和函数，可以根据需要修改。 
  参考 dbgLog.exe.cfg.
